//Check whether a square is black and paint it orange. 
//All other squares on the perimeter should be painted yellow.

if (colorIs(Color.black)) {
    paint(Color.orange);
} else {
    paint(Color.yellow);
}
move();
if (colorIs(Color.black)) {
    paint(Color.orange);
} else {
    paint(Color.yellow);
}
move();
if (colorIs(Color.black)) {
    paint(Color.orange);
} else {
    paint(Color.yellow);
}
turnLeft();
move();
if (colorIs(Color.black)) {
    paint(Color.orange);
} else {
    paint(Color.yellow);
}
move();
if (colorIs(Color.black)) {
    paint(Color.orange);
} else {
    paint(Color.yellow);
}
move();
if (colorIs(Color.black)) {
    paint(Color.orange);
} else {
    paint(Color.yellow);
}
turnLeft();
move();
if (colorIs(Color.black)) {
    paint(Color.orange);
} else {
    paint(Color.yellow);
}
move();
if (colorIs(Color.black)) {
    paint(Color.orange);
} else {
    paint(Color.yellow);
}
move();
if (colorIs(Color.black)) {
    paint(Color.orange);
} else {
    paint(Color.yellow);
}
