//Program Karel to engage the landing systems. 
//Use an if statement and paint(); commands. 
move();
if (colorIs(Color.red)) {
    paint(Color.green);
} else {
    paint(Color.black);
}
move();
if (colorIs(Color.red)) {
    paint(Color.green);
} else {
    paint(Color.black);
}
move();
if (colorIs(Color.red)) {
    paint(Color.green);
} else {
    paint(Color.black);
}
move();
if (colorIs(Color.red)) {
    paint(Color.green);
} else {
    paint(Color.black);
}
